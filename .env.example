# Copia este archivo a .env (o configura la variable en Cloudflare Workers → Settings →
# Variables) cuando actives Cloudflare Web Analytics:
# Dashboard → Analytics → Web Analytics → Add site → copia el "token" del snippet generado.
PUBLIC_CF_ANALYTICS_TOKEN=
